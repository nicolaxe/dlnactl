[project]
name = "simple-dlna"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "async-upnp-client>=0.46.2",
]

[project.scripts]
mini-dlna = "mini_dlna.cli:main"